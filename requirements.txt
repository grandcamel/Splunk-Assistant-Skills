# Splunk Assistant Skills - Python Dependencies

# Shared library (from PyPI)
# Pin to major version to prevent breaking changes
splunk-as>=1.0.0,<2.0.0

# Type hints (Python 3.8+ backports)
typing-extensions>=4.0.0

# JSON Schema validation (optional)
jsonschema>=4.0.0

# Development
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0
